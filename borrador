/* 1 */
:root{
    --color-bg: #0e141b;
    --color-bg-offset: #252526;
    --color-text: #ffffff;
    --color-text-offset: #818a91;
    --color-border: #2d2d2d;
    --color-primary: #ff5277;
    --color-primary-offset: #ff2957;
    --color-secondary: #43a9a3;
}

*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
body {
    font-family: 'Roboto', sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
.encabezado{
    border:  1px solid black;
    min-height: 100vh;
}
.theme_title{
    padding: 1rem;
    font-size: 1em;
    text-transform: uppercase;
    text-align: center;
}
.peleta__colores{
    border: 1px solid red;
    background-color: #2C3E50;
    color: #ECF0F1;
    display: flex;
    flex-direction: column;
   /*  height: 27vh; */
    justify-content: center;
    /* gap: 1vw; */
}
.theme_list{
    /* border: 1px solid blue; */
    display:flex;
    width: 100%;
    justify-content: center;
  /*   white-space: nowrap;
    overflow-y: hidden;
    overflow-x: auto; */
    text-align: center;
    /* padding: 0rem 1.5rem 2rem; */
    gap: 4vw;
}
.theme_container {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 10vw;
    height: 13vh;
    padding: 4px;
    border-radius: 1vw;
    margin-bottom: 20px;
    border: 1px solid green;
}

.theme_name {
    font-size: 16px;
    font-weight: bold;
    margin-bottom: 10px;
}

.theme_palette {
    display: flex;
}

.theme_bg {
    width: 2vw;
    height: 4vh;
    border-radius: 89%;
    margin-right: -7px;
    border: 2px solid white;
}
/* Colores específicos de la paleta */
.theme_bg.bg_primary {
    background-color: var(--color-primary);/* Color principal */
}
.theme_bg.bg_secondary {
    background-color: var(--color-secondary);/* Color secundario */
}
.theme_bg.bg_border {
    background-color: var(--color-border);/* Color de borde */
}
.theme_bg.bg_conten {
    background-color: var(--color-text-offset);/* Color de fondo de contenido */
}
.theme_bg.bg_texto {
   background-color: var(--color-text); /* Color de texto */
}

/* .theme_container{
    border: 1px solid green;
    width: 12vw;
    height: 15vh;
    border-radius: 1vw;
    display: flex;
    flex-direction: column;
  
}
.theme_palette{
    border: 1px solid #ffffff;
    display: flex;
    height: 7vh;
}
.theme_bg{
    width: 9vw;
    height: 5vh;
    border: 1px solid red;
    border-radius: 2vw;
    
} */


/* 2 */
:root {
    --color-bg: #0e141b;
    --color-bg-offset: #252526;
    --color-text: #ffffff;
    --color-text-offset: #818a91;
    --color-border: #2d2d2d;
    --color-primary: #ff5277;
    --color-primary-offset: #ff2957;
    --color-secondary: #43a9a3;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Roboto', sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    overflow-x: auto; /* Agregamos desplazamiento horizontal si es necesario */
}

.encabezado {
    border: 1px solid black;
    min-height: 100vh;
}

.theme_title {
    padding: 1rem;
    font-size: 1em;
    text-transform: uppercase;
    text-align: center;
    white-space: nowrap; /* Evitar el ajuste de línea */
}

.peleta__colores {
    border: 1px solid red;
    background-color: #2C3E50;
    color: #ECF0F1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    white-space: nowrap; /* Evitar el ajuste de línea en los elementos .theme_container */
    overflow-x: auto; /* Barras de desplazamiento horizontal */
}

.theme_list {
    display: flex;
    justify-content: center;
    gap: 2vw;
    border: 1px solid blue;
}

.theme_container {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 12vw;
    height: 13vh;
    padding: 10px;
    border-radius: 1vw;
    margin-bottom: 20px;
    border: 1px solid green;
}

.theme_name {
    font-size: 16px;
    font-weight: bold;
    margin-bottom: 10px;
}

.theme_palette {
    display: flex;
}

.theme_bg {
    width: 2vw;
    height: 4vh;
    border-radius: 89%;
    margin-right: -7px;
    border: 2px solid white;
}

/* Colores específicos de la paleta */
.theme_bg.bg_primary {
    background-color: var(--color-primary);
}

.theme_bg.bg_secondary {
    background-color: var(--color-secondary);
}

.theme_bg.bg_border {
    background-color: var(--color-border);
}

.theme_bg.bg_conten {
    background-color: var(--color-text-offset);
}

.theme_bg.bg_texto {
    background-color: var(--color-text);
}

/* 3 */
:root {
    --color-bg: #0e141b;
    --color-bg-offset: #252526;
    --color-text: #ffffff;
    --color-text-offset: #818a91;
    --color-border: #2d2d2d;
    --color-primary: #ff5277;
    --color-primary-offset: #ff2957;
    --color-secondary: #43a9a3;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Roboto', sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    overflow-x: auto; /* Agregamos desplazamiento horizontal si es necesario */
}

.encabezado {
    border: 1px solid black;
    min-height: 100vh;
}

.theme_title {
    padding: 1rem;
    font-size: 1em;
    text-transform: uppercase;
    text-align: center;
    white-space: nowrap; /* Evitar el ajuste de línea */
}

.peleta__colores {
    border: 1px solid red;
    background-color: #2C3E50;
    color: #ECF0F1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    white-space: nowrap; /* Evitar el ajuste de línea en los elementos .theme_container */
    overflow-x: auto; /* Barras de desplazamiento horizontal */
}

.theme_list {
    display: flex;
    justify-content: center;
    gap: 2vw;
    border: 1px solid blue;
}

.theme_container {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 12vw;
    height: 13vh;
    padding: 10px;
    border-radius: 1vw;
    margin-bottom: 20px;
    border: 1px solid green;
}

.theme_name {
    font-size: 16px;
    font-weight: bold;
    margin-bottom: 10px;
}

.theme_palette {
    display: flex;
}

.theme_bg {
    width: 2vw;
    height: 4vh;
    border-radius: 89%;
    margin-right: -7px;
    border: 2px solid white;
}

/* Colores específicos de la paleta */
.theme_bg.bg_primary {
    background-color: var(--color-primary);
}

.theme_bg.bg_secondary {
    background-color: var(--color-secondary);
}

.theme_bg.bg_border {
    background-color: var(--color-border);
}

.theme_bg.bg_conten {
    background-color: var(--color-text-offset);
}

.theme_bg.bg_texto {
    background-color: var(--color-text);
}
@media screen and (max-width: 768px) {
}

body {
    font-family: 'Roboto', sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    overflow-x: auto;
}

.encabezado {
    border: 1px solid black;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
}

.theme_title {
    padding: 1rem;
    font-size: 1em;
    text-transform: uppercase;
    text-align: center;
    white-space: nowrap;
}

.peleta__colores {
    border: 1px solid red;
    background-color: #2C3E50;
    color: #ECF0F1;
    display: flex;
    justify-content: center;
    align-items: center;
    white-space: nowrap;
    overflow-x: auto;
    width: 80vw;
    max-width: 600px;
}

.theme_list {
    display: flex;
    gap: 2vw;
    flex-wrap: wrap;
    justify-content: center;
}

.theme_container {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 30vw;
    max-width: 150px;
    height: 13vh;
    padding: 10px;
    border-radius: 1vw;
    margin-bottom: 20px;
    border: 1px solid green;
}

.theme_name {
    font-size: 16px;
    font-weight: bold;
    margin-bottom: 10px;
}

.theme_palette {
    display: flex;
}

.theme_bg {
    width: 10vw; /* Ajuste el ancho según sus preferencias */
    height: 10vw; /* Ajuste la altura según sus preferencias */
    border-radius: 50%;
    margin-bottom: 5px;
    border: 2px solid white;
}

/* Colores específicos de la paleta */
.theme_bg.bg_primary {
    background-color: var(--color-primary);
}

.theme_bg.bg_secondary {
    background-color: var(--color-secondary);
}

.theme_bg.bg_border {
    background-color: var(--color-border);
}

.theme_bg.bg_conten {
    background-color: var(--color-text-offset);
}

.theme_bg.bg_texto {
    background-color: var(--color-text);
}

@media screen and (max-width: 600px) {
    .peleta__colores {
        width: 90vw;
    }

    .theme_container {
        width: 80vw;
        max-width: none;
    }

    .theme_bg {
        width: 20vw;
        height: 20vw;
    }
}


/*estilo rral  */
@import url('https://fonts.googleapis.com/css2?family=Agbalumo&family=Epilogue:wght@400;500;700&family=Kumbh+Sans:wght@100;400&family=Poppins:ital,wght@0,100;0,200;0,300;0,600;1,300&family=Rubik:wght@300&family=Space+Mono:wght@700&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    text-decoration: none;
    border: none;
    outline: none;
    scroll-behavior: smooth;
    font-family: 'poppins', sans-serif;
}
html{
 /*    font-size: 62.5%; */
 font-size:clamp(1.1vw,15rem,1vw);
    overflow-x: hidden;
}
body{
   /*  background: rgb(27, 27, 40); */
}
.header{
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    padding: 2vw 7vw;
    background: greenyellow;
    display: flex;
    align-items: center;
    z-index: 100;
}
.logo{
    font-size: 2.5rem;
    color: white;
    font-weight: 600;
    cursor: default;
    margin-right: auto;
}
.navbar .link{
    position: relative;
    font-size: 1.7rem;
    color:white;
    font-weight: 500;
    margin-right: 3.5rem;
}
.navbar .link.active::before{
    content: '';
    position: absolute;
    left: 0;
    width: 100%;
    bottom: 1px;
    height: .2rem;
    background: blue;
}
#icon_palette{
    font-size: 2.4rem;
    color: white;
}
.home{
border: 1px solid green;
}
.section_home{
    min-height: 100vh;
    padding: 10rem 7% 2rem;   
}
.home{
    display: flex;
    align-items: center;
} 
.home .home_content{
    max-width: 44rem;
}
.home_content .home_titulo{
    font-size: 3.2rem;
    font-weight: 700;
    line-height: .3;
}
.home_content .home_nombre{
    font-size: 4.6rem;
    font-weight: 700;
}
.home_content .home_info{
    font-size: 1.6rem;
}
.home_content .social_media a{
    display: inline-flex;
    justify-content: center;
    align-items: center;
    width: 4rem;
    height: 4rem;
    background: transparent;
    border: .2rem solid green;
    border-radius: 50%;
    box-shadow: 0 .2rem .5rem rebeccapurple;
    font-size: 2rem;
    color: aqua;
    margin: 2.5rem 1.5rem 3rem 0;
    transition: .5s ease;
}
.home_content .social_media a:hover{
    background: red;
    color: white;
}
.btn{
    display: inline-block;
    padding: 1.2rem 2.8rem;
    background: yellow;
    border-radius: .6rem;
    box-shadow: 0 .2rem .5rem red;
    font-size: 1.6rem;
    color: white;
    border: .2rem solid transparent;
    letter-spacing: .1rem;
    font-weight: 600;
}
.btn:hover{
    background: transparent;
    color: blueviolet;
    border: violet;
}


